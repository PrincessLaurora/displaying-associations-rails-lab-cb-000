<h1>Artists<h1>

  <ul>
  <% @artists.each do |a| %>
  <ul><%= link_to a.name, artist_path(a)%></ul>
  <% end %>
  <% a.songs.each do |song| %>
  <ul><%=link_to song.title, song_path(song) %></ul>

  <% end %>
  </ul>
